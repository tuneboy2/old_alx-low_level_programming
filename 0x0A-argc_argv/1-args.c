#include <stdio.h>

/**
* main - A function that accepts arguments as parameters
* @argc: Parameter that counts the number of arguments passed
* @argv: Parameter that contains a string of arrays
*
* Return: 0 - SUCCESS
*/

int main(int argc, __attribute__((unused)) char **argv)
{
	printf("%i\n", argc - 1);

return (0);
}
